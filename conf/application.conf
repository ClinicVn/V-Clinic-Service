# This is the main configuration file for the application.
# ~~~~~

# URL and Port
http.port=80
http.address=0.0.0.0

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
#
# This must be changed for production, but we recommend not changing it in this file.
#
# See http://www.playframework.com/documentation/latest/ApplicationSecret for more details.
play.crypto.secret = "d3AQ:c8NOG0Xgzt;HN`lkg1if[/Mja?phfOn?PLDSC1=EysoaTWK8S`<h;Y[cF:J"

# The application languages
# ~~~~~
play.i18n.langs = [ "en", "vn", "jp" ]

# Database configuration
# ~~~~~ 
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`

db.default.driver=com.mysql.jdbc.Driver
db.default.url="jdbc:mysql://localhost/clinic"
db.default.username=root
db.default.password="admin"
db.default.logStatements=true
db.default.jndiName=DefaultDS
jpa.default=mySqlPersistenceUnit

# Logger
# ~~~~~
# You can also configure logback (http://logback.qos.ch/),
# by providing an application-logger.xml file in the conf directory.

# Root logger:
logger.root=ERROR

# Logger used by the framework:
logger.play=INFO

# Logger provided to your application:
logger.application=DEBUG

logger.com.jolbox=DEBUG

play.modules.enabled += "play.modules.swagger.SwaggerModule"
api.version (String) - "1.0" | default: "1.0"
swagger.api.basepath (String) - "http://localhost:9000" | default: "http://localhost:9000"
swagger.api.info = {
   	contact : DungVST,
   	description : "Provice APIs for web server, mobile and other devices",
   	title : Welcome to V-Clinic-Services,
  	termsOfService : "Provice APIs for web server, mobile and other devices",
  	license : Copyright 2016 by V-Clinic,
 	licenseUrl : "http://vclinic.com/"
}